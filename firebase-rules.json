{
  "rules": {
    ".read": false,
    ".write": false,
    "codes": {
      "$roomId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['code', 'title', 'timestamp'])"
      }
    }
  }
}
